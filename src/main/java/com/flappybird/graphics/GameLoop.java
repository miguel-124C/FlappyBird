package com.flappybird.graphics;

public class GameLoop {

    public GameLoop(){
        // var graphics = ;
    }

    public void run(){
        // game.initialize();
        // game.loadContent();

        while ( 0 < 1 ) {
            // var deltaTime = ;
            // game.update(deltaTime);
            // game.draw(deltaTime);
        }
    }

}
